{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the WP-0.2 desktop bootstrap probe.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": []
}
